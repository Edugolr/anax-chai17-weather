language: php

php:
    - "7.2"
    - "7.3"
    - hhvm
    - nightly

sudo: false

git:
    submodules: false
    depth: 1

matrix:
    allow_failures:
        - php: nightly
        - php: hhvm


before_script:
    - make install
    - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
    - chmod +x ./cc-test-reporter
    - ./cc-test-reporter before-build

script:
    - make check
    - make test
    - vendor/bin/codecept run unit --coverage --coverage-xml

after_script:
- mv tests/_output/coverage.xml clover.xml
- ./cc-test-reporter after-build --coverage-input-type clover --id c04cffc3db8710d798d7e62f2c68216ff8dff1f2b4b999d03ade018b36b9327a --exit-code $TRAVIS_TEST_RESULT
